# Default values for gws-dashboards-alerts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# create values.yaml in applicatrions specific folder and declare all varaibles there