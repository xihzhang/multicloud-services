# Default values for dashboards.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

appGrafanaDashboards:  
  # Dashboard Name: 
  #   Name of configMap 
  #   NAME:
  #   Name of key
  #   KEY: 
  webrtc-grafana-cfgmap :
    NAME: webrtc-grafana-dashboard-cfgmap 
    KEY: webrtc-dashboard.json
  webrtc-grafana-states-cfgmap:
    NAME: webrtc-grafana-states-dashboard-cfgmap
    KEY: webrtc-states-dashboard.json