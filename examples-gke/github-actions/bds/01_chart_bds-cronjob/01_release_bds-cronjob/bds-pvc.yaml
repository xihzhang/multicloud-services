apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bds-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: azure-files
  volumeMode: Filesystem
